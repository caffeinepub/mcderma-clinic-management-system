// Re-export all types from useQueries for backward compatibility
export type {
  Appointment,
  Patient,
  PatientView,
  Lead,
  Staff,
  Attendance,
  StaffPermissions,
  WhatsAppTemplate,
  AdminConfig,
  Prescription,
  PrescriptionType,
  ConsultationType,
  PrescriptionData,
  UserProfile,
} from '../hooks/useQueries';
