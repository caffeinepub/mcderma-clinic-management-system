// Re-export from the core-infrastructure package so local imports continue to work.
export {
  useInternetIdentity,
  InternetIdentityProvider,
  type InternetIdentityContext,
  type Status,
} from "@caffeineai/core-infrastructure";
